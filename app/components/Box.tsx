interface BoxProps {
    children: React.ReactNode,
    className?: string,
}

const Box: React.FC<BoxProps> = ({ children, className }) => {
    return (
        <div>

        </div>
    )
}
export default Box